import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function DashboardPage() {
  return (
    <PhasePlaceholder
      phase="Phase 3"
      title="Dashboard"
      summary="The Runway Bar, four stat tiles, recent transactions, and the fast-add row land here."
    />
  )
}
